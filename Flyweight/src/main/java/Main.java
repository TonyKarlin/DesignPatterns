import Factory.Maps.CityMap;
import Factory.Maps.WildernessMap;
import Flyweight.MapView;

public class Main {
    public static void main(String[] args) {
        MapView.launch(MapView.class, args);
    }
}
